export function createTransferRequestKey(){const source=globalThis as{crypto?:{randomUUID?:()=>string;getRandomValues?:(values:Uint8Array)=>Uint8Array}};if(source.crypto?.randomUUID)return source.crypto.randomUUID();const bytes=source.crypto?.getRandomValues?.(new Uint8Array(16));if(!bytes)throw new Error('Secure randomness is unavailable.');return Array.from(bytes,(byte)=>byte.toString(16).padStart(2,'0')).join('');}
