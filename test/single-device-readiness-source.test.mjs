import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{test}from'node:test';const read=(p)=>readFileSync(new URL(`../${p}`,import.meta.url),'utf8');const onboarding=read('src/features/onboarding/OnboardingScreen.tsx');const root=read('app/index.tsx');const auth=read('src/features/auth/AuthScreen.tsx');const bowl=read('src/features/bowl/BowlScreen.tsx');const en=read('src/i18n/en.ts');const hu=read('src/i18n/hu.ts');
test('onboarding is local, skippable, persisted, and teaches the bowl metaphor',()=>{assert.match(onboarding,/AsyncStorage\.setItem\(ONBOARDING_KEY,'true'\)/);assert.match(root,/AsyncStorage\.getItem\(ONBOARDING_KEY\)/);assert.match(en,/same pebbles travel between your two bowls/);assert.match(hu,/Ugyanazok a kavicsok vándorolnak/);});
test('auth and bowl keep guarded accessible interactions',()=>{assert.match(auth,/KeyboardAvoidingView/);assert.match(auth,/keyboardType="email-address"/);assert.match(bowl,/accessibilityLiveRegion/);assert.match(bowl,/t\('bowl\.empty'\)/);assert.match(hu,/A tál most üres\./);});
