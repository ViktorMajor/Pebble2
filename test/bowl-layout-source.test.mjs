import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{test}from'node:test';const read=(p)=>readFileSync(new URL(`../${p}`,import.meta.url),'utf8');const types=read('src/features/bowl/bowlTypes.ts');const layouts=read('src/features/bowl/bowlLayouts.ts');const service=read('src/features/bowl/bowlService.ts');
test('finite identity model stays bounded and spatial',()=>{assert.match(types,/TOTAL_PAIR_PEBBLES = 8/);for(let count=0;count<=8;count+=1)assert.match(layouts,new RegExp(`\\n  ${count}:`));assert.match(service,/visual_seed/);assert.doesNotMatch([types,layouts,service].join('\n'),/rarity|value|collectible|score|ranking/i);});
