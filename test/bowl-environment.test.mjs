import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{test}from'node:test';const source=readFileSync(new URL('../src/features/bowl/bowlEnvironment.ts',import.meta.url),'utf8');
test('bowl light is continuous, local, seasonal, and injectable',()=>{assert.match(source,/getHours\(\).*getMinutes\(\)/s);assert.match(source,/getMonth\(\)/);assert.match(source,/phaseProgress: progress/);assert.match(source,/export function getBowlEnvironment\(date: Date\)/);assert.match(source,/now = \(\) => new Date\(\)/);assert.doesNotMatch(source,/location|weather|latitude|longitude|fetch\(/i);});
